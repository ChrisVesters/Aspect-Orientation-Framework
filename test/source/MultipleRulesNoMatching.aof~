pointcut p() {
	execute int mySquare();
	call int mySqrt();
	call int myPow();
}

pointcut q(int i) {
	call int random(int i);
}

advice c: around p(int i) {
	int retVal = 0;
	printf("BEGIN C!\n");
	retVal = proceed(10);
	printf("END C!\n");
	return retVal;
}
